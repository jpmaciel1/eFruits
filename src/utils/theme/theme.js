'use client';

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000', // Defina sua cor primária aqui
    },
    secondary: {
      main: '#ffffff', // Defina sua cor secundária aqui
    },
  },
});

export default theme;
